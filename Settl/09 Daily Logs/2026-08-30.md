# 2026-08-30

## Today's Goal

Create the foundation for Settl, build the full repository structure, set up backend and frontend, and get Phase 1 running locally with verified tests.

## Worked On

- PRD, TDD, and Phase 1 Foundation plan alignment
- Built repository structure (`apps/api`, `apps/web`, `scripts`)
- Implemented FastAPI backend, Pydantic schemas, and SQLAlchemy models (12 tables)
- Authored & applied Alembic migration `0001_initial_schema`
- Database seeding with default merchant (`demo@settl.ai`), policies, customers, and test cases
- Next.js 15 App Router merchant command center UI (Overview, Recovery Queue, Case Detail, Policies, Evaluation, Login)
- Unit tests & browser verification
- Native local runner (`scripts/run_dev.py`)

## Completed

- [x] Selected Track 03 (AI Revenue Recovery)
- [x] Finalized Settl as project name
- [x] Created build PRD/TDD direction
- [x] **PHASE 2 (CORE RECOVERY ENGINE) 100% COMPLETE:**
  - Revenue event ingestion (`POST /api/v1/events`) with idempotency and dynamic customer provisioning
  - Simulation injector endpoint (`POST /api/v1/events/simulate`)
  - Deterministic baseline risk engine computing calibrated probability and priority
  - Deterministic policy engine enforcing all 6 financial guardrails (attempts, limits, opt-out, cooldown)
  - Recovery case transactional state machine (`NEW` -> `ANALYZING` -> `READY` -> `POLICY_CHECK` -> `APPROVED`/`BLOCKED`/`ESCALATED`)
  - Human review approval/rejection workflows
  - Append-only audit logger
  - UI additions: Simulate Leakage Event modal & Case interactive action buttons
  - Test suite expanded to 22 tests (22/22 passed in 2.51s)
  - Full browser verification of event simulation and escalation flow

## Problems & Solutions

- *Issue:* Passlib 1.7.4 threw an attribute warning with newer bcrypt 4.x.
  *Fix:* Switched to direct, standard `bcrypt` hashing in `auth_service.py` with zero warnings.
- *Issue:* Docker requirement removed for submission.
  *Fix:* Configured direct native Windows execution (Python 3.13 virtualenv + Node.js 22 npm) with unified `scripts/run_dev.py`.
- *Issue:* Supabase transaction pooler (port 6543) threw `DuplicatePreparedStatement` on startup.
  *Fix:* Set `prepare_threshold=None` in SQLAlchemy engine connect args for psycopg3.

## Decisions

- Next.js 15 + App Router + Tailwind CSS for rich modern frontend
- FastAPI + Pydantic v2 + SQLAlchemy 2.0 for high-performance API and ML readiness
- Native local execution without Docker
- Connected live to Supabase PostgreSQL (`aws-0-ap-south-1.pooler.supabase.com:6543`)
- Strict monetary representation in paise as `BIGINT` across all database columns and API contracts
- Deterministic baseline logic guarantees operational reliability even before LLM integration

## Things I Learned

- Operating invariant: **AI recommends; deterministic policy authorizes; Razorpay executes; verified webhook events confirm recovery.**
- Keeping numerical probability inside ML/risk scoring prevents hallucinations and ensures predictable policy gating.
- Razorpay Test Mode limit: 30 Payment Links per business, necessitating strict separation of the 5,000 synthetic event evaluation harness from the live test mode demonstration loop.

## Current Blocker

None. Both services are operational, connected to Supabase, and passing all tests.

## Next Task

Begin **PHASE 3 — AI**:
1. Structured LLM root-cause analysis.
2. Structured recovery decision output.
3. Strict Pydantic schema validation for model outputs.
4. Support model fallback / offline deterministic mode.
